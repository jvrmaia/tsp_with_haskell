Essa foi a ultima tentativa de implementacao da parte dois do trabalho 0, usando a estrutura de dado sugerida pelo senhor.
Nessa versao nao conseguimos finalizar o trabalho e os pre-resultados nao foram melhrores que os anteriores.
Para testar temos um Makefile que gera uma Main executavel.

